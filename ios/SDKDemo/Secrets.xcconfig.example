// Copy to Secrets.xcconfig (gitignored) and fill in.
// The key is delivered out-of-band — never commit it.
NAYUTAL_BASE_URL = https:/$()/example.execute-api.region.amazonaws.com/Prod/
NAYUTAL_API_KEY = your-integration-key-here
